import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF9C6B53);         // Màu nâu nhạt chính
  static const Color primaryLight = Color(0xFFEFE4DC);   // Màu nền sáng nhẹ
  static const Color background = Color(0xFFF9F7F6);      // Nền trắng hồng nhạt
  static const Color scaffold = Color(0xFFF7F7F7);        // Màu nền tổng thể
  static const Color textPrimary = Color(0xFF1A1A1A);     // Đen
  static const Color textSecondary = Color(0xFF5F5F5F);   // Xám đậm
  static const Color white = Colors.white;
  static const Color iconColor = Color.fromARGB(255, 207, 207, 207);
}
