name: do_an_mobile_nc
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.64.0
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  carousel_slider: ^5.1.1
  flutter_rating_bar: ^4.0.1
  diacritic: ^0.1.3
  image_picker: ^1.0.7
  intl: ^0.18.1
  # Performance & UI improvements
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  connectivity_plus: ^5.0.1
  webview_flutter: ^4.4.2
  url_launcher: ^6.2.2
  flutter_staggered_animations: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/data/hcmc-districts.json
    
